<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DocShare Library</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-pastel: #f4f7f6;
            --primary-pastel: #a8dadc;
            --primary-dark: #457b9d;
            --danger-pastel: #f1faee;
            --danger-text: #e63946;
            --text-dark: #2b2d42;
        }
        body { font-family: 'Plus Jakarta Sans', sans-serif; background-color: var(--bg-pastel); color: var(--text-dark); }
        .navbar-pastel { background-color: #ffffff; border-bottom: 2px solid var(--primary-pastel); }
        .btn-pastel-primary { background-color: var(--primary-pastel); color: var(--primary-dark); border-radius: 10px; font-weight: 600; }
        .btn-pastel-primary:hover { background-color: var(--primary-dark); color: #fff; }
        .btn-pastel-danger { background-color: var(--danger-pastel); color: var(--danger-text); border-radius: 10px; border: 1px solid rgba(230,57,70,0.2); }
        .alert-pastel { border-radius: 12px; border: none; box-shadow: 0 4px 10px rgba(0,0,0,0.03); }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-pastel mb-4 py-3">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">🌿 DocShare</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Trang chủ</a></li>
                    {% if session.get('username') %}
                    <li class="nav-item"><a class="nav-link" href="/upload">Đăng tài liệu</a></li>
                    {% endif %}
                </ul>
                <div class="d-flex align-items-center gap-3">
                    {% if session.get('username') %}
                        <span class="small">Xin chào, <b>{{ session['username'] }}</b></span>
                        <a href="/logout" class="btn btn-pastel-danger btn-sm px-3">Đăng xuất</a>
                    {% else %}
                        <a href="/login" class="btn btn-pastel-primary btn-sm px-3">Đăng nhập</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>

    <div class="container">
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="alert alert-{{ category }} alert-pastel fade show">{{ message }}</div>
                {% endfor %}
            {% endif %}
        {% endwith %}
        {% block content %}{% endblock %}
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
